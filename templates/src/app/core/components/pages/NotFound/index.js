import React from 'react';
import MainContent from '../../modules/MainContent';

const NotFound = () => (
  <MainContent title="Página não encontrada">
    <p>Não foi possível encontrar uma página com o caminho informado.</p>
  </MainContent>
);

export default NotFound;
